{
  "name": "@tgra/tasks",
  "version": "1.0.0-alpha1.2",
  "main": "out/index.js",
  "repository": "https://github.com/cmumme/tegra.git",
  "author": "Camren Mumme <me@typechecked.dev>",
  "license": "GPL-3.0-only",
  "private": false,
  "scripts": {
    "build": "yarn tsc",
    "clean": "rm -r src/*.js && rm -r src/*.d.ts",
    "prepublishOnly": "yarn build"
  },
  "devDependencies": {
    "@tgra/builder": "^1.0.4",
    "typescript": "^5.2.2"
  }
}
